# 15-Minute Demo
1. Show incident, service, log and KB JSON files.
2. Explain Incident Commander and three specialist agents.
3. Run `python reset_state.py`, `python check_setup.py`, `python app.py`.
4. Use `INC-1001`.
5. Before approval, show that `services.json` is unhealthy.
6. Approve with `y`.
7. After execution, show the service changed to healthy.
8. Run `python reset_state.py` before the next demo.
