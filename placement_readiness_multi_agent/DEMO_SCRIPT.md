# 12-Minute Demo
1. Show `students.json`, `roles.json`, `learning_resources.json`.
2. Explain manager vs specialist agents in `agents_app.py`.
3. Run `python check_setup.py`.
4. Run `python app.py` with the default request.
5. Open `outputs/rohan_ai_engineer_plan.md` if the save tool was called.
6. Explain: agentic AI is useful because the manager decides which specialist/tool to call and combines independent outputs.
